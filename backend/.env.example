# Copy to `backend/.env` for local development.

# Comma-separated list of allowed frontend origins (CORS). Use * in dev.
ALLOWED_ORIGINS=*

# Anthropic API key — used only by the LLM interpretation layer (wired up Day 4).
# Leave blank until then; the app falls back to a template note without it.
ANTHROPIC_API_KEY=
